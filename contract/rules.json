{
  "V1": { "severity": "blocking",      "check": "series labelled Revenue has presentation_basis == umsatzerloese" },
  "V2": { "severity": "blocking",      "check": "single unit within a series" },
  "V3": { "severity": "note_required", "check": "consolidation perimeter change between adjacent years" },
  "V4": { "severity": "note_required", "check": "GKV/UKV switch mid-series" },
  "V5": { "severity": "note_required", "check": "line moves more than 15 percent YoY" },
  "V6": { "severity": "note_required", "check": "cost ratio breaks trend more than 5pp" },
  "V7": { "severity": "blocking",      "check": "unmapped label used in a charted series" },
  "V8": { "severity": "blocking",      "check": "Aktiva equals Passiva" },
  "V9": { "severity": "advisory",      "check": "negative equity position present" },
  "V10": { "severity": "note_required", "check": "a subtotal must equal the sum of its component lines" },
  "V11": {
    "severity": "blocking",
    "implemented": true,
    "enforced_in": ["py", "ui"],
    "materiality_threshold_revenue_pct": 0.01,
    "materiality_threshold_eur_floor": 1000000,
    "materiality_threshold_rationale": "A stated one-off is material when it exceeds the higher of 1% of the applicable revenue basis and the EUR 1m absolute floor, so the requirement scales with issuer size without treating sub-EUR-1m items as material.",
    "check": "a series labelled EBITDA states its earnings_basis; adjusted requires every adjustment traced to a stated Lagebericht amount; reported requires a footnote for each material stated Lagebericht one-off"
  },
  "V12": {
    "severity": "blocking",
    "check": "EBITDA sums the PL_GKV-7 family; every child is mapped or explicitly asserted absent"
  }
}
