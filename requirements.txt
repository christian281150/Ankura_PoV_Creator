# Core pipeline
openpyxl>=3.1.5          # xlsx read/write - normalise layer
pydantic>=2.9            # contract/models.py validation
pdfplumber>=0.11.4       # PDF table + text extraction
pdfminer.six>=20221105   # pdfplumber backend
pandas>=2.2.3            # DataFrame helpers, CSV export

# Acquisition
httpx>=0.27              # register + web fetch
beautifulsoup4>=4.12     # HTML parsing
lxml>=5.3                # bs4 backend
playwright>=1.50.0       # extractor - imported at module load; Edge via CDP
rich>=13.9.4             # extractor CLI output
customtkinter>=5.2.0     # ur_gui.py

# Render
python-pptx>=1.0.2
pillow>=11.0.0           # image normalise, perceptual hash, pdfplumber

# Dev
pytest>=8.3
<<<<<<< Updated upstream
ruff>=0.7
=======
ruff>=0.7

# Note: the GUI uses Microsoft Edge via Playwright, pre-installed on Windows
# 10/11. If unavailable: playwright install chromium
# Build-only: pyinstaller>=6.0.0, then build.bat
>>>>>>> Stashed changes
