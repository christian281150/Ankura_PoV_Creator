# Core pipeline
openpyxl>=3.1.5          # xlsx read/write - normalise layer
pydantic>=2.9            # contract/models.py validation
pdfplumber>=0.11.4       # PDF table + text extraction

# Acquisition (lanes B and C)
httpx>=0.27              # register + web fetch
beautifulsoup4>=4.12     # HTML parsing
lxml>=5.3                # bs4 backend

# Render (lane F)
python-pptx>=1.0.2

# Dev
pytest>=8.3
ruff>=0.7

playwright>=1.48         # bundesanzeiger extractor - imported at module load
rich>=13.9               # extractor CLI output
pillow>=11.0             # image normalise + perceptual hash (lane img)