# ── Setup ────────────────────────────────────────────────────────────────────
# Install all dependencies:
#   pip install -r requirements.txt
#
# The GUI uses Microsoft Edge via Playwright.  Edge is pre-installed on every
# Windows 10/11 machine, so no additional browser download is needed.
# If Edge is unavailable, install Chromium as a fallback:
#   playwright install chromium

# ── Direct dependencies ───────────────────────────────────────────────────────
playwright>=1.50.0          # async browser automation (Edge / Chromium via CDP)
pdfplumber>=0.11.4          # PDF table extraction (wraps pdfminer.six)
pandas>=2.2.3               # DataFrame helpers used by CSV export
rich>=13.9.4                # terminal output, progress bars (CLI mode)
customtkinter>=5.2.0        # modern CustomTkinter GUI (ur_gui.py)
openpyxl>=3.1.0             # Excel (.xlsx) workbook creation

# ── Key transitive dependencies (pinned for reproducibility) ──────────────────
pdfminer.six>=20221105      # core PDF parsing backend used by pdfplumber
Pillow>=11.0.0              # image handling used by pdfplumber

# ── Build-only (not needed at runtime) ────────────────────────────────────────
# pyinstaller>=6.0.0        # uncomment to install the build tool
#                           # then run:  build.bat
